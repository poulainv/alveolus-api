<%= form_for(@admin_webapp) do |f| %>
  <% if @admin_webapp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_webapp.errors.count, "error") %> prohibited this admin_webapp from being saved:</h2>

      <ul>
      <% @admin_webapp.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

      <div class="field">
        <%= f.label :title %>
        <%= f.text_field :title ,:class => "input-xlarge"%>
      </div>


      <div class="field">
        <%= f.label :url %>
        <%= f.text_field :url ,:class => "input-xlarge"%>
      </div>


      <div class="field">
        <%= f.label :caption %>
        <%= f.text_field :caption, :class => "input-xlarge" %>
      </div>

      <div class="field">
        <%= f.label :description %>
        <%= f.text_area :description, :class => "input-xxlarge" %>
      </div>

        <div class="field">
          Identifiant Vimeo <%= f.text_field :vimeo_id, :placeholder=>"Optionnel" %>
        </div>

       <div class="field">
          Identifiant Facebook <%= f.text_field :facebook_id, :placeholder=>"Optionnel" %>
        </div>

       <div class="field">
          Identifiant G+ <%= f.text_field :twitter_id, :placeholder=>"Optionnel" %>
        </div>

       <div class="field">
          Identifiant Twitter <%= f.text_field :gplus_id, :placeholder=>"Optionnel" %>
        </div>

        <div class="field">
          Validation <%= f.check_box :validate %>
        </div>

        <div class="field">
          Promotion <%= f.check_box :promoted %>
        </div>

        <div class="field">
          Suggestion <%= f.check_box :featured %>
        </div>
    <!--   <div>
        <%= f.label :tag_list, "Tags (séparé par virgules)" %>
        <%= f.text_field :tag_list, :class => "input-large" %>
      </div> -->

      <div class="field">
        Photo <%= f.file_field :photo %>
      </div>

      <div class="actions">
        <%= f.submit :class => "btn btn-primary btn-default" %>
      </div>

    </div>


<!--   <div class="actions">
    <%= f.submit %>
  </div> -->
<% end %>
